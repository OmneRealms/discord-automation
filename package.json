{
  "name": "discord-automation",
  "version": "1.0.0",
  "description": "OmneRealms Discord bot — bridges bugs/suggestions to GitHub issues",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dedup": "node scripts/dedup-audit.js",
    "dedup:fix": "node scripts/dedup-audit.js --fix --yes",
    "dedup:all": "node scripts/dedup-audit.js --all",
    "dedup:all:fix": "node scripts/dedup-audit.js --all --fix --yes"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.0",
    "discord.js": "^14.0.0",
    "dotenv": "^16.0.0"
  }
}
